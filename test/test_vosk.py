from nerd_dictation.vosk import *

# `vosk.py` can't really be tested in an
# environment that doesn't have model files or
# an audio source so the recommendation is not
# to touch it without a ten-foot pole. If you're
# reading this, you probably edited `vosk.py`
# and should strongly consider reverting your
# changes.